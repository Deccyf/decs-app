{
  "name": "decs-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Dec's Tracker \u2014 pay, cash flow, bills, collections and games. A single-file offline PWA.",
  "scripts": {
    "build": "node build.js",
    "pretest": "node build.js",
    "test": "node --test test/*.test.js",
    "test:calc": "node build.js && node --test test/calc.test.js",
    "test:ui": "node build.js && node --test test/ui.test.js"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
