{
  "name": "decs-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Dec's Tracker — pay, cash flow, bills, collections and games. A single-file offline PWA.",
  "scripts": {
    "test": "node --test test/*.test.js",
    "test:calc": "node --test test/calc.test.js",
    "test:ui": "node --test test/ui.test.js"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
